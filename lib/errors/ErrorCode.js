'use strict';

module.exports = {
	INTERNAL : Symbol('internal'),
	MISSING_PARAMETER: Symbol('missing_parameter'),
	EXPIRED_TOKEN : Symbol('token expired'),
	INVALID_CREDENTIALS: Symbol('invalidcreds')
};
